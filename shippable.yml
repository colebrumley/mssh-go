language: go
go:
  - release
before_install:
  - export GOPATH=$SHIPPABLE_GOPATH
install:
  - go get -t -d -v ./... && go build -x -o mssh ./...
script:
  - cp mssh shippable/buildoutput/
archive: true
