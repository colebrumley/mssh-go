language: go

go:
  - release

env:
  - CGO_ENABLED=0

before_install:
  - export GOPATH=$SHIPPABLE_GOPATH

install:
  - go get -t -d -v ./... && go build -x -o mssh -a -installsuffix cgo ./...

script:
  - cp mssh shippable/